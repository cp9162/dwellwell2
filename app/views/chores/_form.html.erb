<%= form_for(@chore) do |f| %>
   <% if @chore.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@chore.errors.count, "error") %> prohibited this house from being saved:</h2>

      <ul>
      <% @chore.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
    <% end %>


  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title%><br />
  </div>
  <div class="field">
    <%= f.label :is_done %><br />
    <%= f.boolean :is_done %><br />
  </div>
  <div class="field">
    <%= f.label :assignment %><br />
    <%= f.text_field :assignment %><br />
  </div>
  <div class="field">
    <%= f.label :completed_on %><br />
    <%= f.date_select :completed_on %><br />
  </div>


  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

